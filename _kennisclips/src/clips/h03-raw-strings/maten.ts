import { NAAST_ELKAAR } from "../../stijl/werkblad";

// De maten staan in stijl/werkblad.tsx (indeling NAAST_ELKAAR): code links, console rechts.
export const CODE_VAK = NAAST_ELKAAR.codeVak;
export const CONSOLE_VAK = NAAST_ELKAAR.consoleVak;
export const CODE = NAAST_ELKAAR.code;
export const UIT = NAAST_ELKAAR.uit;
